meta {
  name: Create Holder in IssuerService
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/issuer/admin/api/admin/v1alpha/participants/aXNzdWVy/holders
  body: json
  auth: apikey
}

auth:apikey {
  key: x-api-key
  value: {{issuer_apiKey}}
  placement: header
}

body:json {
  {
      "did": "{{participant_context_did}}",
      "holderId": "{{participant_context_did}}",
      "name": "{{participant_context_id}} tenant"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
